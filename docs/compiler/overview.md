# Compiler Overview

Nuvsha is a compiled framework. Your `.nuv` files cannot run directly in the browser. 

When you start the development server (`npm run dev`) or build for production (`npm run build`), Nuvsha processes your files and turns them into pure, high-performance JavaScript.
