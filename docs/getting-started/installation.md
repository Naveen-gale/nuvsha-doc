# Installation

To use Nuvsha, you need [Node.js](https://nodejs.org/) installed on your system. We recommend the latest LTS version.

You can verify your Node.js installation by running this in your terminal:

```bash
node -v
```

If you see a version number (like `v18.x.x` or higher), you're ready to start!
