# Testing

Nuvsha is built using Node.js's native `node:test` runner.

## Running Tests

To run the automated tests for a Nuvsha application, use:

```bash
npm test
```

This will execute unit tests, compiler verification tests, and interactive runtime simulations.
